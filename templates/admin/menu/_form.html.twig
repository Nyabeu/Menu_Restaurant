{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
            {{ form_errors(form) }}
        </div>

        <div class="row justify-content-end">
          <div class="col-6">
            {{ form_row(form.name) }}
          </div>
          <div class="col-6">
            {{ form_row(form.price)}}
          </div>
        </div>
        <div class="form-group row">
          <div class="col-12" id="some-custom-id">
            {{ form_row(form.brochureMenu) }}
          </div>
        </div>

        <div class="form-group row">
          <div class="col-12" id="some-custom-id">
            {{ form_row(form.description) }}
          </div>
        </div>


    <button class="btn btn-success offset-11 col-1 mt-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
