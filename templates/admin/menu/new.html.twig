{% extends 'base.html.twig' %}

{% block title %}Nouveau Menu{% endblock %}

{% block body %}
  <div class="container mt-4">
    <h2 class="text-center text-success" style="text-shadow: 5px 5px 5px tomato">Créer un nouveau Menu</h2>

    <div class="mt-3">{{ include('admin/menu/_form.html.twig', {'button_label': 'Créer'}) }}</div>

    <a href="{{ path('admin.menu.index') }}" class="btn btn-primary mb-5">Retour à la liste</a>
  </div>
{% endblock %}
