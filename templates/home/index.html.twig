{% extends "base.html.twig" %}

{% block title %}Restaurant Afro{% endblock %}

{% block body %}
<div class="container mt-3">
    <div class="row flex">
        {% for menu in menus %}
          <div class="col-3">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title">
                  <a href="{{ path('menu.show', {id: menu.id, slug: menu.slug}) }}">{{ menu.name }}</a>
                </h5>
                <p class="card-text">{{ menu.title }} <br> {{ menu.description}}</p>
                <p class="text-primary" style="font-weight: bold; font-size: 2rem">{{ menu.price }} €</p>
              </div>
            </div>
          </div>
        {% endfor %}
    </div>
    {# <div class="blockPictureTaro">
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/taro.jpg') }}" alt="taro" width="100%" height="214" class="rounded"></a></p>
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/cous.jpg') }}" alt="couvert" width="100%" class="rounded-circle"></a></p>
    </div>
    <div class="blockPicturePoulet">
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/poulet.jpg') }}" alt="poulet" width="100%" height="200" class="rounded"></a></p>
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/couvert.png') }}" alt="couteauCuillere" width="100%" height="215"></a></p>
    </div>
    <div class="blockPicturePoisson">
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/poisson.jpg') }}" alt="poisson" width="100%" height="215" class="rounded"></a></p>
      <p><a href="{{ path('menu.index') }}"><img src="{{ asset('images/couv.jpg') }}" alt="poserCouvert" width="100%" class="rounded-circle"></a></p>
    </div> #}
</div>

{% endblock %}
